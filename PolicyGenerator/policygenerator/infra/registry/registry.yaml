apiVersion: imageregistry.operator.openshift.io/v1
kind: Config
metadata:
  name: cluster
spec:
  defaultRoute: true
  managementState: Managed
  replicas: 2
  rolloutStrategy: Recreate
  storage:
    emptyDir: {}
 
